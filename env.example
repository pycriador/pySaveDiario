# Copie este arquivo para .env e ajuste conforme o ambiente.
FLASK_ENV=development
SECRET_KEY=change-this-secret-key
TOKEN_EXPIRATION_MINUTES=60

# SQLite local
DB_ENGINE=sqlite
SQLITE_DB_NAME=app.db

# Parâmetros globais (MariaDB/PostgreSQL)
DB_USER=app_user
DB_PASSWORD=app_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pysavediario

# Exemplos de DSN completos
# DATABASE_URL=mariadb+pymysql://usuario:senha@localhost:3306/pysavediario
# DATABASE_URL=postgresql+psycopg://usuario:senha@localhost:5432/pysavediario

